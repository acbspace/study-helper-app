/**
 * The web dashboard reuses the shared design tokens, so its colour, spacing, and type match
 * the mobile app rather than drifting into a separate look.
 */

export { lightTheme as theme, radius, spacing, typography } from '@study-league/design-tokens';
