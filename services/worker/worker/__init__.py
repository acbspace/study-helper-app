"""Study League background jobs.

Jobs import domain services from the API package rather than reimplementing rules, so
there is exactly one definition of session and scoring behaviour.
"""

__version__ = "0.1.0"
